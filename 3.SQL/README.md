# Tasks

* For all customers with more than 3 events with status = 'failure' , report the
customer name and their number of failures.

* The result should be ordered by the number of failures in descending order.

The solution is in the file called: **applicant_query.sql**
